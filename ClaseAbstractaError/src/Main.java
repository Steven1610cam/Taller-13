public class Main {
    public static void main(String[] args) {
        // Intento de instanciar la clase abstracta (esto generará un error de compilación)
        Animal animal = new Animal(); // Error de compilación: Animal es abstracta; no se puede instanciar
    }
}